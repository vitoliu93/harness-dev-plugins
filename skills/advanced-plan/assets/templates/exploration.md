# Exploration

Field notebook of discoveries about the **code/task** (facts, not feelings).
APIs and quirks, where things live, undocumented constraints, observed failures
and their causes, useful commands. Append, never rewrite history.

## [<session-id>]
1. <discovery — a fact about the repo/system, with file:line or command if relevant>
2. <gotcha / constraint / "X looks wrong but is intentional because Y">
