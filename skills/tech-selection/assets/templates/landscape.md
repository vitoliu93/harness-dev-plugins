# Landscape / 方案调研: <subject>

> The option space you decide *from*. Maturity: ✅ production-ready · ⚠️ weak/partial · ❌ no good option.

## Problem definition
<Restate the problem and what the chosen approach must produce — inputs, outputs, constraints.>

## Pipeline decomposition (the reframe — draw this FIRST)
> Draw this before any option tables. It reveals whether the problem is one model or a multi-stage pipeline, and prevents the single-model trap (BGM reverse isn't one model — it's separate → identify → characterize). The stages below become the sub-questions.

```
<input>
  ├── <stage A: job> ──► <output>
  ├── <stage B: job> ──► <output>
  └── <stage C: job> ──► <output>
                          ▼
                    <combined result>
```

- **Key reframe / non-obvious note:** <e.g. "most of the hard metrics live in stage C, not B" — the insight that changes the option set>

## Options per stage / sub-question

### SQ1 (stage A) — <sub-question>
| Option | How it works | Maturity | Risk / limitation | Fit to criteria |
|--------|--------------|----------|-------------------|-----------------|
| <A1> | <one line> | ✅/⚠️/❌ | <main risk> | <good/poor — why> |
| <A2> | <one line> | … | … | … |

### SQ2 (stage B) — <sub-question>
| Option | How it works | Maturity | Risk / limitation | Fit to criteria |
|--------|--------------|----------|-------------------|-----------------|
| <…> | … | … | … | … |
