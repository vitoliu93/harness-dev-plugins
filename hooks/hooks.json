{
  "description": "Redirect inline use of noisy delegated skills (table-driven in skill-guard.sh) from the main context to a subagent — exa-code/html-doc/lark-* -> general-skills-executor, ask-ai -> ai-second-opinion. Subagent contexts are exempt via agent_id.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Skill|Read",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/skill-guard.sh",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
